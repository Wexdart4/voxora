[package]
name = "voxora-geometry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Projective camera geometry, epipolar math, and triangulation for Voxora"

[lints]
workspace = true

[dependencies]
voxora-core = { path = "../voxora-core", version = "0.1.0" }
voxora-math = { path = "../voxora-math", version = "0.1.0" }
voxora-vision = { path = "../voxora-vision", version = "0.1.0" }
